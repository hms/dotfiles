# echo "Processing env files"

# special handling for setting $DOTFILES -- this needs to run before anything else
source $HOME/Devel/dotfiles/zsh/zshenv.env

for sh_file in `ls $DOTFILES/zsh/**/*.env`; do
  # echo "zshrc processing " $sh_file
  source $sh_file
done

# vim:ft=sh:
