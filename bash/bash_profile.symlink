# login shells should do at least everything that interactive shells do.
[ -f ~/.bashrc ] && source ~/.bashrc



# vim:ft=sh:

[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*
