# mvim has a terminal mode which seems to be a pretty good way of getting a
# proper vim build on the terminal.

# Turned off since I don't use TMUX
# Tune back on as needed
# which mvim > /dev/null && alias vim='mvim -v'
